#include <iostream>

using namespace std;

int main() {
    bool isAlive = false;
    if (isAlive)
        cout << "It lives!" << endl;
    else if (!isAlive)
        cout << "Back to the drawing board..." << endl;

    return 0;
}